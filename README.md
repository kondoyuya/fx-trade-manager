# 🦀 FX Trade Manager

FX の取引記録・日次収支をカレンダー形式で管理できるデスクトップアプリです。  
Rust（Tauri）と React を使用しています。

---

#### 使用する技術
| 項目      | 使用技術                            |
| ------- | ------------------------------- |
| フロントエンド | React, TypeScript, Tailwind CSS |
| バックエンド  | Rust, Tauri                     |
| データベース  | SQLite                          |
| パッケージ管理 | pnpm                            |
| ビルド     | Vite                            |


## 📁 プロジェクト構成
```
fx-trade-manager/
├── src/ # React フロントエンド
├── src-tauri/ # Rust (Tauri) バックエンド
├── package.json # React/Tauri 共通設定
├── tauri.conf.json # Tauri 設定ファイル
└── profits.db # SQLite データベース（実行後に生成）
```

## 開発環境セットアップ

### 1. 必要なツールをインストール

#### 共通
- [Node.js](https://nodejs.org/)（推奨: v20 以上）
- [pnpm](https://pnpm.io/)（推奨）
- [Rust](https://www.rust-lang.org/tools/install)
- [Visual Studio Build Tools](https://visualstudio.microsoft.com/ja/downloads/)
  > ※ Windows では Rust のビルドに C++ ビルドツールが必要です  
  > → 「C++ Build Tools」を選んでインストール

#### Rust コンポーネント
```
rustup update
cargo install create-tauri-app
```

### 2. 依存関係をインストール
プロジェクトルートで以下を実行
```
pnpm install
```

### 3. 開発サーバーを起動
```
pnpm tauri dev
```
アプリがビルドされ、Tauri のウィンドウが立ち上がります
React 側の変更はホットリロードされます

### 4. ビルド(実行ファイルの作成)
```
pnpm tauri build
```
実行ファイル（例：fx-trade-manager.exe）が
src-tauri/target/release/ に生成されます。

