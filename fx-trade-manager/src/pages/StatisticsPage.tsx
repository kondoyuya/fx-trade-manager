import React from "react";
import LabelManager from "../components/LabelManeger.tsx";

const StatisticsPage: React.FC = () => {
  return (
    <div className="flex flex-col items-center">
        <LabelManager />
    </div>
  );
};

export default StatisticsPage;
